export enum ComponentTypesEnum {
  undefined,
  jumbo,
  button,
  navBar,
  pageTemplate,
  text,
  image,
  groupingContainer,
};

export const enumerate: {[index: string]: any} = ComponentTypesEnum;